<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Member">
 	
 	<insert id="mem_in">
	 	insert into member(mem_id, mem_pwd, mem_name, mem_email, mem_tel, mem_class)
	 	 values(#{mem_id}, #{mem_pwd}, #{mem_name}, #{mem_email}, #{mem_tel}, #{mem_class})
 	</insert>
 	
 	<select id="mem_select" resultType="int">
 		select count(*) from member where mem_id = #{mem_id} and mem_pwd = #{mem_pwd}
 	</select>
 	
 	<select id="mem_select_id" resultType="int">
 		select count(*) from member where mem_id = #{mem_id}
 	</select>
 </mapper>
 