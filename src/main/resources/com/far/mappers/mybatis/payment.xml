<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Payment">
	
	<select id="getCoupons" resultType="coupon">
		SELECT * FROM coupon
		INNER JOIN couponpersonal
		ON coupon.couponname = couponpersonal.couponname
		WHERE memid = #{mem_id}
	</select>
	
	<select id="getMenu" resultType="room">
		SELECT * FROM room
		WHERE roomNum = #{roomNum}
	</select>
	
	<select id="getStore" resultType="store">
		SELECT * FROM store WHERE storeNum = #{store_num}
	</select>
	
	<select id="get_couponIssue" parameterType="hashmap" resultType="coupon">
		SELECT * FROM coupon c
		INNER JOIN couponpersonal cp
		ON c.couponname = cp.couponname
		WHERE cp.couponname = #{coupon_name} AND memId = #{mem_id}
	</select>
	
	<insert id="insert_coupon">
		INSERT INTO couponpersonal(couponnum, couponname, memId)
		VALUES (#{coupon_num}, #{coupon_name}, #{mem_id})
	</insert>
	
	<select id="getMember" resultType="member">
		SELECT * FROM member
		WHERE memId = #{mem_id}
	</select>
	
</mapper>