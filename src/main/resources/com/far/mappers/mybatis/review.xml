<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Review">
 	
 	<!-- 리뷰 등록 -->
 	<insert id="set_review">
 		insert into review(review_num,memId,store_num,review_rating, review_cont, review_recommend,review_date)
 		values(
 		NUM_SEQ.nextval,
 		'abc123',
 		10,
 		#{review_rating},
 		#{review_cont},
 		1,
 		sysdate
 		)
 	</insert>
 	
 	<!-- 리뷰 목록  -->
 	<select id = "get_review" resultType="com.far.dto.ReviewDTO">
 		select * from review where store_num = 10 order by review_num desc
 	</select>
 	
 	<!-- 리뷰삭제 -->
 	<delete id="del_review">
 		delete from review where review_num=#{review_num}
 	</delete>
 	
 	<!-- 리뷰 수정 -->
 	<update id="edit_review">
 		update review set review_cont = #{review_cont}, review_rating= #{review_rating}, review_date = sysdate where review_num=#{review_num}
 	</update>
 	
 	<!-- 리뷰 한 개 정보 -->
 	<select id="get_update_review" resultType="com.far.dto.ReviewDTO">
 		select * from review where review_num=#{review_num}
 	</select>
 	
 </mapper>